<nav class="navbar navbar-dark bg-dark fixed-bottom bottom-navbar">
  <div class="container d-flex justify-content-around align-items-center">
    <% if user_signed_in? %>
      <%= link_to conversations_path, class: "nav-link text-white #{'active' if current_page?(conversations_path)}" do %>
        <i class="bi bi-chat-dots-fill"></i>
        <span>Messages</span>
      <% end %>

      <%= link_to new_post_path, class: "nav-link text-white" do %>
        <i class="bi bi-plus-circle"></i>
        <span>Ajouter</span>
      <% end %>

      <%= link_to '#', class: "nav-link text-white" do %>
        <i class="bi bi-search"></i>
        <span>Recherche</span>
      <% end %>

      <%= link_to user_path(current_user), class: "nav-link text-white d-flex flex-column align-items-center" do %>
        <% avatar_display_url = current_user.avatar_url.presence || "https://placehold.co/40x40/E0E0E0/000000?text=User" %>
        <%= image_tag avatar_display_url, alt: "User Avatar", class: "rounded-circle mb-1", style: "width: 40px; height: 40px; object-fit: cover;" %>
        <span>Profil</span>
      <% end %>
    <% else %>
      <%= link_to new_user_session_path, class: "nav-link text-white" do %>
        <i class="bi bi-box-arrow-in-right"></i>
        <span>Login</span>
      <% end %>
    <% end %>
  </div>
</nav>
